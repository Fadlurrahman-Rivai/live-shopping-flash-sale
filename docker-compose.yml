services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: live-shopping-frontend
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped
    networks:
      - live-shopping-network

networks:
  live-shopping-network:
    name: live-shopping-network