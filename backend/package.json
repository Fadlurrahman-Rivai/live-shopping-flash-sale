{
  "name": "live-shopping-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/db.js && node --check src/server.js"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.1.0",
    "express": "^4.21.2",
    "express-rate-limit": "^8.6.2",
    "pg": "^8.13.1",
    "redis": "^6.2.1"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5",
    "supertest": "^7.0.0"
  }
}